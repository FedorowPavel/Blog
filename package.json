{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^6.0.3"
  },
  "scripts": {
    "start": "lerna run start:dev",
    "test": "lerna run test",
    "instal": "lerna bootstrap --hoist"
  }
}
